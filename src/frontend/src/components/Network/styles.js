import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({

  text: {
    marginTop: '75px',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
  }
}))