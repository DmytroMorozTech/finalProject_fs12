import {makeStyles} from '@material-ui/core/styles'

export default makeStyles(() => ({

  likeMini: {
    color: '#fff',
    fontSize: 12,
    backgroundColor: '#0a66c2',
    borderRadius: 30,
    cursor: 'pointer',
    marginTop: '3px',
    marginRight: '3px'
  }
}))