import {makeStyles} from '@material-ui/core/styles'

export default makeStyles(() => ({

  likeMini: {
    color: '#fff',
    fontSize: 15,
    backgroundColor: '#0a66c2',
    borderRadius: 30,
    cursor: 'pointer'
  }
}))