import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({

  fullscreenFalse: {
    display: 'flex',
    justifyContent: 'center',
    height: '100%',
    width: '100%',
    margin: '10px auto'
  }
}))
